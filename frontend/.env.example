# FastAPI base URL. Leave empty to use the independent frontend mock mode.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Set to "true" to force mock mode even when an API URL is configured.
NEXT_PUBLIC_USE_MOCK=false